version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
    nodejs: "24"

mkdocs:
  configuration: packages/native/mkdocs.yml
  fail_on_warning: true

python:
  install:
    - requirements: packages/native/docs/requirements.txt
